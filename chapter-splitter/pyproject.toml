[project]
name = "chapter-splitter"
version = "1.0.0"
description = "Deteccion y fragmentacion de capitulos en instruccionales de BJJ"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = "-v --tb=short"
