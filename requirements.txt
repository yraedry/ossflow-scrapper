# Lean dependencies for the oracle-only build.
# No torch, no demucs, no easyocr, no opencv. ffmpeg comes from the base image.

fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
pydantic>=2.5
httpx>=0.27
selectolax>=0.3.21
